using System;
using System.Collections.Generic;
using System.Linq;
using System.Security.Principal;
using System.Text;
using System.Threading.Tasks;

namespace BvdR_Lib.Game.Priority
{
    internal class TurnChoiseState : BaseState
    {
        //choice draw or play, discard or sauron, roll or noroll
        //ChoosingDrawPriority(BvdR_UserInput input)
        //{
        //    RequestInput(input);
        //}

        //private List<string> options = new() { "draw", "play" };

        //async private void RequestInput()
        //{

        //    object responce = 0;
        //    while (!(responce is not null && responce.GetType() == typeof(string)) && options.Contains((string)responce))
        //        responce = await input.MakeChoice(options.ToArray());
        //    string output = (string)responce;
        //    if (output == "draw")
        //    {

        //    }

        //}
    }
}
